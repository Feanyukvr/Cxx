#include <iostream>;
using namespace std;
#include<string>;
int main3() {
	/*
	
	int score = 0,a=0,c;
	for (a = 0;;) {
		cout << "请输入一个分数：";
		cin >> score;

		if (score < 20) {
			cout << "第一\t" << score << endl;
		}
		else if (score < 40) {
			cout << "第二\t" << score << endl;
		}
		else {
			cout << "第三\t" << score << endl;
		}
	}

	cout << "积分啊";
	cin >> c;
	switch (c) {//不可以是区间，只能为整型，字符型，结构清晰，效率高
	case 10:
		cout << "非常好" << endl;
		break;
	case 9:
		cout << "常好" << endl;
		break;
	default:
		cout << "垃圾" << endl;
		break;
	}

	while (c <10 ) {
	}当什么时继续

	do {
	}while()做，当什么时继续做

	break跳出循环
	continue下一轮


	三目运算符
	int a = 10, b = 5,c;
	c = a > b ? a:b;
	cout << "c=" << c << endl;
	
	goto语法
	1.。。
	goto FLAG;
	2.。。
	FLAG:
	3.。。


		*/





	system("pause");
	return 0;
}